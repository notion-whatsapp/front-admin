import { InputFieldBaseDirective } from './input-field-base.directive';

describe('InputFieldBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFieldBaseDirective();
    expect(directive).toBeTruthy();
  });
});
